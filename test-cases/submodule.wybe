module privatetest {
    proc hidden use !io !print("private proc in a private module") end
    public proc semi_hidden use !io !print("public proc in a private module") end
}

public module publictest {
    proc semi_visible use !io !print("private proc in a public module") end
    public proc visible use !io !print("public proc in a public module") end
}
