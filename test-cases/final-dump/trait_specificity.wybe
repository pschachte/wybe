# Verify dispatch selects the concrete procedure candidate when the concrete type is known.

use trait_import_support.specificity_printable,
    trait_import_support.specificity_scalar

!println(render(trait_specificity_scalar(42)))
