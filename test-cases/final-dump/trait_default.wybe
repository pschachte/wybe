# Verify trait dispatch uses a default method when none is overridden.

use trait_import_support.default_greeter,
    trait_import_support.default_silent

def greet_through_trait(x:trait_import_support.default_greeter): string = greet(x)

!println(greet_through_trait(trait_default_silent))
