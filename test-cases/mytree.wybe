pub type tree { pub empty | node(left:tree, key:int, right:tree) }

pub def printTree(t:tree) use !io {
  if { t = node(?l, ?k, ?r) ::
           !printTree(l)
           !print(" ")
           !print(k)
           !print(" ")
           !printTree(r)
  }
}
